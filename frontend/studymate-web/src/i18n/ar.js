/* ==========================================================================
   Arabic - empty on purpose.

   Every key falls back to en.js while this object is empty, so the app is
   word for word what it was before the interface was extracted. The
   translation is batch 2, and it can be filled in a piece at a time: a key
   added here is translated, a key still missing reads as English.
   ========================================================================== */

export default {}
